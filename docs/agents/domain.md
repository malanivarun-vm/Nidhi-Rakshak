# Domain docs

This repo uses the single-context layout.

Before exploring a domain, read root `CONTEXT.md` if present and relevant ADRs under `docs/adr/`.

Use glossary terms from those documents when naming domain concepts. Create context or ADR documents lazily when a domain term or architectural decision needs recording.
